import React, { useState } from "react";
import { useInput } from "./Hooks/useInput";

export default function GameForm() {
  return (
    <form className="form">
      <input />
      <input />
      <bottom>Save</bottom>
    </form>
  );
}
